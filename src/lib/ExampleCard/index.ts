// Make import statement easier to read by exporting component from index.ts 
// so we can import directly from the ExampleCard folder, instead of from ExampleCard/ExampleCard.tsx
export * from "./ExampleCard";